name: Auto-add to project

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  call:
    uses: wyre-technology/.github/.github/workflows/auto-add-to-project.yml@49a63247da45ecf2e184f33c5d7d871777bfa6b0  # pinned (warden C-4) — bump via PR not in-place edit
    secrets: inherit
